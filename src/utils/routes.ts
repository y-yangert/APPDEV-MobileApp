interface Routes {
  LOGIN: string;
  REGISTER: string;
  HOME: string;
  PROFILE: string;
  ERROR: string;
}

const ROUTES: Routes = {
  LOGIN: 'Login',
  REGISTER: 'Register',
  HOME: 'Home',
  PROFILE: 'Profile',
  ERROR: 'Error',
};

export default ROUTES;
